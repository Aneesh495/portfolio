# portfolio

React (Vite) + Express in one repo. Drizzle schema in `shared/`.

```bash
npm install
npm run dev
```

`npm run build` · `npm run start` · `npm run db:push`

Database and session env vars: see `server/db.ts`, `drizzle.config.ts`.
